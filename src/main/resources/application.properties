#Banner image
spring.banner.image.location=logo.jpg

# disabling thymeleaf cashe (ONLY for development!)
#spring.thymeleaf.cache=${SPRING_THYMELEAF_CACHE}

####
spring.datasource.initialization-mode=${SPRING_DATASOURCE_INITIALIZATION-MODE}
spring.datasource.data=${SPRING_DATASOURCE_DATA}

spring.jpa.hibernate.ddl-auto=${SPRING_JPA_HIBERNATE.DDL-AUTO}
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}

#spring.datasource.schema=classpath:db/h2-schema.sql

spring.datasource.continue-on-error=${SPRING_DATASOURCE_CONTINUE-ON-ERROR}
# testi scriptu vykdym\u0105 net klaidos atveju

####
logging.level.org.hibernate.SQL=${LOGGING_LEVEL_ORG_HIBERNATE_SQL}
logging.level.org.hibernate.type.descriptor.sql=${LOGGING_LEVEL_ORG_HIBERNATE_TYPE_DESCRIPTOR_SQL}

#spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql = ${SPRING_JPA_PROPERTIES_HIBERNATE_FORMAT-SQL}

spring.data.web.pageable.default-page-size= ${SPRING_DATA_WEB_PGEABLE_DEFAULT-PAGE-SIZE}